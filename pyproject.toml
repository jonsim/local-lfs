[project]
name = "local-lfs-dev-workspace"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit==4.5.1",
    "robotframework==7.4.2",
    "robotframework-robocop==8.8.0",
]

[tool.uv]
package = false
